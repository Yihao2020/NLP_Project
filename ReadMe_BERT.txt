1. Keep the dataset csv file and python code in the same file
2. Name the dataset as 'train.csv'
3. Comment out the section of code you do not want to execute (e.g. if you want to execute the two-dense layer on top of BERT, comment out the portion where one-dense layer is created and the portion where only pre-trained model is being used
4. Run the code from the command line using the command: python BERT_Classification.py